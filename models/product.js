class Product {
    constructor(id, title, imageUrl, address){
        this.id = id,
        this.title = title,
        this.imageUrl = imageUrl,
        this.address = address
    }
}

export default Product 